plugins:
    source:
        - module: example
sources:
    xarr_example:
        driver: 'example-data-source'
        args:
            zarr_path: "example.zarr"
    filled_example:
        driver: 'bluesky-msgpack-catalog'
        args:
            paths: ["msgpack_data/*.msgpack"]
